# Caesar Cipher
A command-line cryptography tool that encodes and decodes text using a Caesar cipher substitution algorithm with configurable shift offsets.

### Tech / Concepts
* Python 3
* Modular arithmetic (modulo operator `%` for alphabet wrapping)
* String manipulation and non-alphabetical character preservation

### Quickstart
`python main.py`
