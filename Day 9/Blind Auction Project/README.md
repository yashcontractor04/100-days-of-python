# Secret Blind Auction
A CLI auction program that collects blind bids from multiple users and determines the highest bidder.

### Tech / Concepts
* Python 3
* Hash maps / Dictionaries for key-value record storage
* Custom key-based maximum lookup (`max(dict, key=dict.get)`)

### Quickstart
`python main.py`
