# Turtle Race Game
An interactive desktop betting game built using Python's Turtle module demonstrating object state, multiple class instances, and event/coordinate loops.

### Tech / Concepts
* Multiple independent `Turtle` class instances with distinct state
* GUI user input handling via `screen.textinput()`
* Game loop condition handling and boundary detection

### Quickstart
`python main.py`
