# CLI Quiz Game
A trivia game engine built with custom OOP data models and an orchestrator managing question queues, state evaluation, and scoring.

### Tech / Concepts
* Custom class modeling (`Question`, `QuizBrain`)
* Data parsing from structured JSON-like collections into object instances
* State machine managing queue progression

### Quickstart
`python main.py`
